$Server = "IWKSUSEPAHSQL01.prod.cloud" 
$Port = "5985"

$t = New-Object Net.Sockets.TcpClient
$t.Connect($Server,$Port)
    if($t.Connected)
    {
        "Port $Port is operational"
    }
    else
    {
        "Port $Port is closed, You may need to contact your IT team to open it. "
    }
